import { AppBar, Toolbar } from '@mui/material';

const Header = () => {
  return (
    <AppBar position="sticky">
      <Toolbar>
        <h1>اینترنت برای همه</h1>
      </Toolbar>
    </AppBar>
  );
};

export default Header;
